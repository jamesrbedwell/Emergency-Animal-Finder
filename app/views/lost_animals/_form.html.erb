<%= form_for @animal, html: { multipart: true } do |f| %>
  <% if @animal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@animal.errors.count, "error") %> prohibited this pet from being saved:</h2>
 
      <ul>
      <% @animal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
 
  <div class="field">
    <%= f.label :species %><br>
    <%= f.text_area :species %>
  </div>
    <div class="field">
    <%= f.label :date_lost %><br>
    <%= f.text_area :date_lost %>
  </div>
    <div class="field">
    <%= f.label :location_lost %><br>
    <%= f.text_area :location_lost %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.file_field :image_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>